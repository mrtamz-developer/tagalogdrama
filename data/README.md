# Catalog data

`series.json` is the temporary public catalog source for the static frontend.

Production requirements before launch:

- Replace placeholder metadata with verified titles and licensed content.
- Move private/admin-managed catalog data behind an authenticated backend.
- Never put private video URLs, payment credentials, or admin secrets in this directory.
